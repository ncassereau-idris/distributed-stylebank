defaults:
    - base_data_conf

folder: data
vgg_file: "${data.folder}/vgg16-397923af.pth"
monet: "${data.folder}/monet_jpg"
style_quantity: -1
photo: "${data.folder}/photo_jpg"

store_transformed: False
preload_transformed: False

load_model: False
weights_subfolder: weights
bank_weight_filename: "bank_{0}.pth"
model_weight_filename: "model.pth"
encoder_weight_filename: "encoder.pth"
decoder_weight_filename: "decoder.pth"
