defaults:
    - base_data_conf

folder: data
vgg_file: "${data.folder}/vgg16-397923af.pth"
monet: "${data.folder}/monet_jpg"
style_quantity: -1
photo: "${data.folder}/photo_jpg"

store_transformed: True
preload_transformed: True

load_model: False
weights_subfolder: weights
bank_weight_filename: "${data.weights_subfolder}/bank_{0}.pth"
model_weight_filename: "${data.weights_subfolder}/model.pth"
encoder_weight_filename: "${data.weights_subfolder}/encoder.pth"
decoder_weight_filename: "${data.weights_subfolder}/decoder.pth"
